{ lib, ... }:

{
  # Placeholder for hardware configuration — replace with real hardware config via:
  #   nixos-generate-config --show-hardware-config > config-override/hardware-configuration.nix
  # Then override at rebuild time with:
  #   --override-input settings path:./config-override
}
